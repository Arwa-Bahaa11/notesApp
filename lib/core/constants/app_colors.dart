import 'package:flutter/material.dart';

class AppColors {
  // Primary Brand Colors
  static const Color primaryColor = Color(0xFF2D2A70);
  static const Color accentColor = Color(0xFF6C63FF);
  
  // Neutral Colors for Students (Reading-friendly)
  static const Color backgroundColor = Color(0xFFFFFFFF);
  static const Color scafoldBackground = Color(0xFFF8F9FD); // Soft blue-white
  
  // Feedback Colors (Evaluates QA/Testing requirements)
  static const Color errorColor = Color(0xFFE57373);  
  static const Color successColor = Color(0xFF81C784);
  
  // Text Colors
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF757575);
}